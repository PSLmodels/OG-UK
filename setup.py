# This file exists for backwards compatibility with older tooling.
# The canonical package configuration is in pyproject.toml.
# Both `pip install -e .` and `uv sync` work correctly.
from setuptools import setup

setup()
